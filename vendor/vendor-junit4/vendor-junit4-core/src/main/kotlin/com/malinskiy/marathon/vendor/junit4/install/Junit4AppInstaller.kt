package com.malinskiy.marathon.vendor.junit4.install

import com.malinskiy.marathon.vendor.junit4.configuration.Junit4Configuration

class Junit4AppInstaller(conf: Junit4Configuration) {
    /**
     * Transfer local classpath to remote device if device is not local
     */
    suspend fun install() {
    }
}
