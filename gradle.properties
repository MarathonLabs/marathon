# Marathon version. Gradle sets `project.version` from this automatically.
# `Deployment.getVersion()` reads it (or `GIT_TAG_NAME` on release CI) and
# reassigns `project.version` with a `-SNAPSHOT` suffix outside release mode.
version=0.11.0
